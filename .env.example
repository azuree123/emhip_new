# Copy to .env and fill in real values before running `docker compose up`.
# SQL Server requires 8+ characters including at least 3 of: uppercase, lowercase, digit, symbol.
MSSQL_SA_PASSWORD=Ch4ngeMe!2026
