import * as React from 'react';
export interface VideoCameraProps {
  className?: string;
  style?: React.CSSProperties;
}
export declare const VideoCamera: React.FC<VideoCameraProps>;
export default VideoCamera;
